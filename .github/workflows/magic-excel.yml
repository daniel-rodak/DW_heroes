name: Magic for excel
run-name: ${{ github.actor }} is testing out GitHub Actions 💡
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
